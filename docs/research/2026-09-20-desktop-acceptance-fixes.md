# 复杂桌面验收问题修复

本批对应 `2026-09-20-desktop-complex-acceptance.md` 的 B01–B09。用户要求不升版本，因此开发树与本机交付继续使用 **1.0.50**。

## 已修改的行为

| 问题 | 修改 | 验证边界 |
|---|---|---|
| B01 会话桥隔离导入失败 | `agent_session_bridge.py` 在导入共享桥模块之前设置项目根路径 | 使用真实 `python -I` 子进程验证插话入队、查询及取消请求；安装目录验证随交付执行 |
| B02 两轮后续问超出 64 KiB | Electron 仅传递最近 12 轮的问答和来源证据，去掉 UI reasoning/trajectory 副本；conversation 桥使用 8 MiB 请求限额 | 大中文历史请求能进入桥；Runtime 的持久消息历史继续独立保存 |
| B03 写后反复读取导致 stalled | Write/Edit 实际写入后逐字节读回，提供 `file_bytes_readback` 回执，明确 `persistence_only` | 四个文件连续写入正常 completed，无冗余验证门；回执不保证公式或推导正确 |
| B04 原生应用无法观察、启动及聚焦 | 用户点名且匹配唯一窗口时绑定任务 SourceRef 和读/修改范围；保留窗口和动作权限检查；用 Windows App Paths 解析已安装应用；必要时通过输入线程交接聚焦并再次确认实际前台 | 模拟窗口的 Runtime 来源检查、注册路径启动、聚焦交接与释放测试通过；修后原生应用完整任务待用户空闲后验收 |
| B05 CSV 格式和结果声称错误 | Write/Edit 对 CSV 检查列数，保存 UTF-8 BOM/CRLF；交付规则区分落盘、计算与实际应用验证 | 修正 CSV 合计列及产物报告；独立复算订单与区域合计。真实 Excel 导入未完成，不能记为通过 |
| B06 失败计划显示完成 | Todo 支持 blocked/cancelled，跨上下文保留阻塞原因；计划门不再要求把受阻/取消步骤改为 completed；UI 显示受阻数量和状态 | 状态、持久计划协议与展示回归；模型仍需据实际目标判断是否完成 |
| B07 工具活动错误表述 | 计划、提问、窗口观察与枚举不归入读文件；权限拒绝显示 Blocked，汇总不声称命令已经运行 | 独立工具行与混合工具组回归 |
| B08 内部权限问题覆盖标题 | 权限决策控制轮不改写自动任务标题 | 持久存储回归；普通追问的原有标题策略保留 |
| B09 漏关键路径和预算口径 | 完成核验要求覆盖并行分支、零余量路径和版本预算口径；修正本例决策文档 | 从两版 JSON 独立重算日期、依赖、费用及所有最长路径；不在 Harness 内硬编码本例任务 |

## 修正产物

原始失败文件保存在 `artifacts/desktop-cu-20260920/originals/`；修正文件保存在 `artifacts/desktop-cu-20260920/corrected/`。

- `sales-data.csv`：每行 10 列，合计公式位于 H/I/J，647 字节，UTF-8 BOM/CRLF。
- `report.md`：订单毛额 2440、退款 280、净额 2160；区域净额华北 770、华东 700、华南 690。只陈述已生成和已验证的内容。
- `decision.md`：v1 关键路径 A–C–D–E–F，11 工作日，10 月 6 日完成，原预算余额 1200；v2 两条关键路径 A–B–D–E–F 和 A–C–D–E–F，均 10 工作日，10 月 5 日完成，预算余额 200。
- `independent-verification.json`：独立计算结果。

## 验证与交付记录

新回归在修改前复现了隔离导入、请求大小、写入回执、CSV、Todo 状态、任务标题、应用绑定、应用解析及聚焦错误。Write 的四文件循环回归进一步验证不会重复进入验证门。

首次全量 Node 检查发现隔离测试环境缺少新引入的 `bridgeHistoryTurns`，已补齐测试依赖。另四项首次进程失败，在独立重跑和后续全量运行中通过。后续 Node 全量 **258 test files** 通过。

首次 Python 全量为 2501 passed / 4 failed：两处旧测试替身未接受请求限额参数、一处纯结构观察测试受到无目标绑定路径影响、一处工作区测试得到非预期 cwd。已修改参数替身及无目标绑定路径，并停止并行 pytest。权限续接另补回归，确保用户此前点名的目标跨控制轮保留，不采用助手回答中的应用名称。受影响的 **128 项串行回归全部通过，205.53s**。

随后串行全量为 **2505 passed / 1 failed / 424.44s**，同一工作区测试在组件扫描中出现 MemoryError，因此撤回“仅由并行临时目录清理导致”的判断。该测试的 repo-local 临时目录继承了父开发仓库，组件扫描会访问与测试无关的真实源文件；单独运行也耗时 30.55s。测试现创建独立临时 Git 仓库并忽略模拟截图，保持原有工作区、来源、隐私和能力断言，定向通过为 **1 passed / 3.09s**。没有为了消除测试失败而修改生产工作区解析器或放宽断言。

最终 `npm run sync` 内全量验证通过：**Python 2506 passed / 6 条既有 Pillow 弃用提示 / 302.32s；Node 258 test files；lint、全部 TypeScript 检查通过**。日志为 `data/sync-desktop-fixes-1.0.50-delivery-20260920.log`。此前失败或受干扰运行不作为交付通过证据。

本机安装同步与安装目录独立验证仍在执行，最终结果将在本节更新。

## 实机验收状态

用户在修复过程中明确表示“我还在使用，先完成后台验证”，因此暂停前台输入。修正 CSV 曾在真实 Excel 导入向导显示 UTF-8 识别及正常中文预览，但导入中断，未取得完整表格及公式计算结果；未生成新的原生 xlsx。停止、插话按钮和多轮复杂桌面任务也尚未在修后安装版重新操作，不能以单元回归代替这部分验收。

成果核验提示词用于新建任务；既有任务已冻结的历史系统提示不作追溯改写。会话桥、CSV 写入、Todo 和桌面工具的实际实现修复不依赖改写历史消息。

# Magic Pointer 个人 Agent PRD

日期：2026-09-23。本文是产品需求与架构收敛稿，供后续实现和验收使用；它不把尚未验收的能力写成已经交付，也不替换现有的 [办公与设计 PRD](PRD.md) 和 [Harness 事实源](docs/design/MAGIC_POINTER_HARNESS_20260811.md)。

## 一句话

**Magic Pointer 是住在用户电脑上的一个执行者。用户把一件事和相关材料交给它，它能继续找资料、操作应用、修改产物、等待变化；用户随时可以纠正或接管；最后拿到可检查的结果。**

任务可以只需一轮，也可以跨小时、跨重启。它们都由 MP 自己的 Runtime 执行。用户只看见一个 Agent；内部子任务只是它处理复杂工作的手段。

这里的「个人」来自持续理解这个用户交给它的目标、材料、修改意见和工作习惯，不来自偷偷收集所有聊天记录。这里的「Agent」要求把事情做到约定结果，不能只生成一段建议。

## 为什么要写这份 PRD

用户在 [Grok 分享对话](https://x.com/i/grok/share/13c91c933dfb4b5fa50f3931d19cf134)里明确了五件事：不以代购、退订和支付任务起步；中国的微信、钉钉、飞书等信源常常封闭；对外是一个 Agent，不做 IM 聊天壳或一排 Bot；自动化和分发的小任务应在后台跑，别抢用户电脑；希望实际能力远超过几张主动建议卡。

Grok 给出的「显式投喂、后台管家、工作台」方向可以保留，但它没有回答决定产品成败的几个问题：任务从输入到完成具体怎么流转？用户正在操作电脑时谁能占用鼠标和键盘？程序崩了之后怎样知道哪些动作已经做过？怎样证明修改落在对的对象上？为什么一个小任务会用掉许多轮模型调用？这份 PRD 回答这些问题。

[Today 官方页面](https://today.ai/)已经把长期记忆、主动提醒、云电脑、工具执行都列为自己的方向。我们不能靠假定别人「只会提醒」来定义优势，也不能用功能数量代替体验。MP 要在**用户自己的桌面、用户当下指定的对象、跨应用可核对的真实交付**上做深，并把普通任务的成本降到与成熟 Harness 同一量级。

## 用户实际会怎样用

### 交任务

用户在微信或钉钉看见「把这两版报价按最终口径整理成一页，周五前给我」。他可以圈这条消息、拖入两个文件，或从应用的分享入口交给 MP，并说「先做草稿，别发」。MP 当场固定这次所指的画面和对象，记录两份文件的身份、用户说的目标，以及「草稿」和「发送」的区别。看不到群内更早的决定，就说明缺哪条，让用户补指或在已授权范围内查找。不能因为读到一句话，就声称已经接管了整段聊天。

### 执行中

MP 可以在用户继续工作的同时读文件、检索、整理、写草稿和运行不抢焦点的工具。工作台显示「正在核对两个报价的版本」「已修改第 4 页两个文本框，正在读回」，并提供暂停、纠正、接管。若下一步必须操作当前桌面的鼠标键盘，而用户正在使用它，任务等待合适时机或请用户接管；不能暗中抢焦点。用户中途说「刚才 A 只是参考，改 B」，这句话进入原任务，下一次写入前生效。

### 交付与继续

结果是一份可编辑草稿、改好的原生文件、准确的回答，或经用户当轮明确授权后已经发送的内容。MP 展示做了什么、依据哪份材料、改了哪个对象、哪些地方没有验证。用户修改草稿后，Agent 继续在同一版本上工作。任务结束、应用重开、次日再问，都能找到同一任务和产物。

### 主动工作

用户可以指定「每天 18 点汇总我选的任务」「关注这个文件夹，报价改变就更新草稿」。每项关注都显示来源、触发条件、上次处理内容和停止按钮。微信、钉钉、飞书若没有可用且获授权的消息接口，就由用户转发、圈选或分享具体内容；将来某应用真的提供可靠事件源，再由对应适配器接入。PC 关机时本地 Agent 不运行，重新打开后处理积压事项并说明错过了什么，不假装全天在线。

## 产品必须守住的六件事

| 需求 | 用户看得见的行为 | 程序必须做到的事 |
| --- | --- | --- |
| 指得准 | 「改这里」只改用户所指的段落、单元格或形状 | 冻结当时画面和对象；读取更多内容时保留历史指代；写前重获当前对象，找不到就停 |
| 任务不断 | 插话、关闭窗口、重开应用后继续原工作 | 已有 EventSession 保存目标、材料、进度、产物版本、待决定事项和动作结果；模型摘要不能充当唯一任务状态 |
| 后台让路 | 用户继续用电脑，MP 不抢鼠标和焦点 | 文件、原生接口和连接器优先；物理桌面动作只有一个所有者，用户活动时等待或接管；等待不算任务失败 |
| 做完才算 | 不把「工具返回成功」当成「任务完成」 | 每个交付结果都有完成条件；每次写入记录目标及读回证据；未知结果明确显示待核对 |
| 记得有边界 | 记住用户认可的做法，也允许查看、修改、忘掉 | 任务记录、当前工作上下文、长期个人记忆分开；长期记忆只来自用户加入的材料和认可过的结果，用时按需找回 |
| 费用可控 | 简单任务不靠多轮侦察和重复读文件完成 | 首轮只带本任务实际可用的工具和必要材料；工具返回短结果加可继续读取的引用；记录每轮原始、缓存和未缓存 token 以及结果 |

## Agent 架构：保留一个循环，把事实放在循环外

不再造第二个 Planner、第二套任务数据库或新的关键词路由。沿用现有 `EventSession`、`Context`、`ToolRegistry`、`ActionBroker`、`Artifact` 与桌面适配器，把它们之间的职责收紧：

1. **任务记录是事实源。** 保存用户的目标、交付条件、材料及各自角色、当前进度、最新纠正、已执行动作和待核对结果。任务状态由程序更新。模型可以建议下一步，不能靠一句「我已经完成」改变状态。
2. **模型只负责选择下一步。** 每轮拿到简短的任务状态、当前相关证据和可用工具，决定回答、继续读取、操作、等待或问用户。短任务和长任务使用同一个循环。只有真正独立、并行后能缩短关键路径的工作才开内部子任务；不为了显得像团队而多开 Agent。
3. **材料由来源接口按需供应。** 一次指向的冻结画面说明「当时是什么」；Office、网页和应用的当前读取说明「现在是什么」。原文、整页和大工具结果保存在本地，需要哪一段再读哪一段。压缩时保留来源入口、未完成事项和已验证结果，不要求模型凭摘要记住一切。
4. **工具由真实上下文决定。** 已知是两个文件，就给文件读取和精确编辑；已指向 Excel，再给 Excel 能力；没有桌面目标，不默认带一整套桌面工具。模型仍是自然语言任务的路由者；显式按钮动作可直接调用确定性工具。工具不够时，再从真实工具目录加载。
5. **动作与结果逐项结算。** 读操作可并行。写入、发送、删除、物理输入按目标与输入所有权协调。动作前重获目标，动作后由对应工具做一次足够的读回或应用层验证，并返回它验证了什么。多个写入不能共用一个笼统的「已验证」布尔值。
6. **工作台只投影任务事实。** 默认展示当前目标、正在做的下一步、产物和需要用户判断的事项；原始工具轨迹可以点开。进度不是模型编写的故事，也不按预设七步假装有百分比。

这个设计保留 MP 已有的 FrameLease、持续指代、来源范围、权限、可编辑产物和恢复能力。它要求减少模型每轮需要理解的表面积；没有证据表明必须加一个新的「认知中台」。

## 现在最需要修的架构问题

以下是 2026-09-23 的源码与本机记录，不是对所有任务的统计结论。

**一，小任务带了太多工具。** 一次真实模型执行的「读 `approved.txt`，只改 `order.txt` 的 Quantity 行」成功样本用了 4 次模型请求、21,178 个输入 token、680 个输出 token，耗时 13.7 秒；15,360 个输入 token 被缓存读取。该会话的每个请求都带 **42 个工具定义**，序列化约 **13,408 字符**，包括桌面、MCP、后台 Agent 等与两文件编辑无关的工具。本机证据为 `data/runtime/harness-evals/precise-edit-repeat-check/report.json` 和相应 `agent-sessions/*.jsonl`；代码入口见 [`ToolRegistry.schemas`](electron/runtime/tools.ts) 与 [`runAgent`](electron/runtime/agent.ts)。缓存减了价格，但没有减少四次往返，也没有让模型更容易选对工具。

**二，任务完成仍有模糊地带。** 当前 Runtime 用任务级 `wrote`、`verified` 状态生成收尾回执；工具成功后若设置了验证标记，就可能把「某一步已核对」投影成整项任务已核对。用户要的是每个指定目标和交付条件分别成立。把完成条件绑到目标对象、产物和对应读回记录；未覆盖的条件显示未完成。见 [`runAgent` 的动作与回执](electron/runtime/agent.ts)。

**三，「有活动」不等于「有进展」。** 当前循环中一次新的非错误工具结果可以刷新活动期限，重复同一调用才容易触发停滞保护。换着办法反复读取，却没有更接近交付，也可能继续花 token。保留长任务的滚动期限，但续期依据应是发现了新事实、完成一个子目标、推进了产物，或正在等待明确的外部条件。连续无进展时先改变办法，再明确求助或暂停；不能把正常的长任务按轮数硬切断。见 [`runAgent` 的进度与重复调用处理](electron/runtime/agent.ts)。

**四，大结果仍容易跟着对话走。** 当前只对若干读取工具做相同内容的去重，工具结果超过 64K 字符才换成文件引用或截取。一个不足 64K 的长结果依旧可被后面每轮反复带入。结果工具应默认返回够决策的摘要、精确位置和续读入口；需要全文时由 Agent 指定页、区域或范围再次读取。见 [`projectContextMessages`](electron/runtime/agent_services.ts) 与 [`runAgent` 的工具结果处理](electron/runtime/agent.ts)。

这些是可定位的成本与正确性问题。不能据此宣称 MP 已比 Pi 贵几倍：目前没有同一模型、同一任务、同一结果条件的 Pi 对照。下一步比较必须同口径，失败运行的花费也要计入。

## 成本要求

- 每次模型请求记下：系统提示、工具定义、任务状态、材料片段、历史消息各占多少；Provider 报告的输入、输出、缓存读取、缓存写入及缺报字段分别显示。不能把缓存命中率当作任务完成率，也不能把原始 token 当作账单金额。
- 用户已给出明确文件或对象时，首轮不让 Agent 用 `Glob`、`list_windows`、`Context.list` 重新发现它们。需要探索时再加载相应工具。
- 读取工具默认给可定位的必要片段；相同来源且版本未变时复用已有结果。写工具应尽量把自身读回证据一起返回，让模型不必为了「显得谨慎」多开一轮无意义验证。Office 等应用层结果仍须真正读回应用状态。
- 动态内容放在提示的后部，稳定的系统说明和工具定义保持稳定以利缓存；不要为了追求缓存率，把不相关工具永远放进前缀。
- 用 Pi 做**同模型、同 Provider、同文件任务**对照。目标：MP 的正确率不低于 Pi，成功任务的未缓存输入加输出 token 中位数不高于 Pi 的 1.25 倍；超出时先解释额外调用买到了什么结果，再增加功能。桌面独有任务单独看「每个已验证结果花费」，不假造 Pi 的桌面对照。

## 失败时用户应该看到什么

| 发生的事 | MP 的行为 |
| --- | --- |
| 微信群、文件或网页只读到一部分 | 显示已读范围和缺口，继续在授权范围内找；拿不到就请用户补充，不编造上下文 |
| 用户改变目标或新指了一处 | 把纠正排到下一次写入前，已完成动作和产物保留，待做步骤重新判断 |
| 程序中断或电脑重启 | 从任务记录恢复；先确认外部状态。结果未知的发送或删除不自动重做 |
| 桌面输入被用户占用 | 非侵入工作继续，物理输入步骤等待；状态写「等你空出桌面」并允许用户接管 |
| 工具连续工作却没有推进目标 | 停止重复调用，换有依据的路径或指出具体阻碍；不以换工具名刷新任务寿命 |
| 产物写了但未能读回 | 显示「已执行，待核对」及目标，不显示完成；保留继续验证和恢复入口 |
| 周期关注没有新变化 | 保持安静。需要报告的变化只形成一次任务，不反复提醒同一事实 |

## 验收：只看真实结果

第一组用于判断架构，而不是制造大量测试文件。每例保留真实任务日志、模型/版本、工具清单、token、耗时、产物和失败原因；只对失败或表现不稳定的例子复测。

1. **普通文件任务。** 同一模型、同一两文件任务分别交给 MP 和 Pi；检查答案或磁盘最终字节、模型轮次、工具调用、原始与未缓存 token。MP 不应为了已给出的文件名再枚举全目录，编辑工具已证明的局部结果不再被重复验证。
2. **封闭聊天入口。** 用户显式指向微信／钉钉的一条需求并加入两份附件；MP 区分最终口径与旧版，做出可编辑结果，缺少上文时具体索取。没有应用提供的原生消息身份时，不宣称已读完整会话。
3. **真实办公修改。** 在打开的 PowerPoint、Word 或 Excel 中只改指定对象；原应用读回目标与未选区域。Figma 必须等真插件 ID 和原生应用验收，协议测试不算完成。
4. **后台与接管。** 用户继续使用电脑时，MP 完成文件型工作而不抢焦点；需要 GUI 输入时等待。用户中途纠正目标、停止、重启后继续，已发生的动作不会重复。
5. **有限主动关注。** 用户只授权一个文件夹或一个时间点；变化触发一次草稿，停用后无新任务，未变化不提醒。PC 关闭期间没有运行的时段如实呈现。

上述第一组完成前，不以新模型、更多插件、更多主动提醒补救失败。验收报告必须区分：协议通过、真实模型完成、原生应用读回、安装版交付。只跑能识别具体风险并改变下一步行动的检查；本文是文档，不要求运行产品测试。

## 实施先后

1. **先止住浪费与假完成。** 在现有 Runtime 缩小首轮工具面，减少大结果重复进入上下文，把每个目标的完成条件及证据做成可读回执；记录与 Pi 同口径的基线。保留现有权限和来源约束。
2. **再证明三条完整链。** 先做「显式消息与附件→可编辑草稿」「两份材料→精确 Office 修改」「文件任务→后台完成」；三条都走同一个任务循环和工作台，不各建一套流程。
3. **然后把长任务做稳。** 在真实跨小时任务上验证等待、插话、停止、重启恢复与未知动作处理。现有后台子任务和 W12 关注能力继续沿同一个 EventSession，避免另造调度中心。
4. **最后扩应用与主动性。** 哪个应用的真实用户任务最多、现有适配能力不足，就补那个 SurfaceAdapter；可靠的官方接口出现时再扩主动消息来源。用户认可的重复做法才变成可编辑工作方法，不自动授予永久执行权。

这里的「先后」是产品优先级，不给短任务或长任务设置产品边界。模型、插件和 OS 接口可以持续升级，但每次升级都必须改善用户得到的结果、成本或可靠性。

## 此阶段不做

- 默认读取全天聊天、屏幕或剪贴板；没有信源权限却宣称自动收取别人发来的任务。
- 代购、支付、理财等需要处理金钱的任务作为首批主路径。
- 一排对外 Bot、IM 聊天式产品壳，或把困难任务交给外部 Harness 执行。
- 为了架构好看增加第二套状态存储、第二个 Agent loop、通用评分中台、无消费者的校验文件或测试脚手架。
- 宣称「支持任意 Windows 应用」「已超过 Today」「已比 Pi 便宜」；这些都要由相同条件下的真实结果支持。

## 现有事实与外部依据

- 本项目已交付与未验收的边界：[当前状态](docs/STATUS.md)、[现有 PRD](PRD.md)、[Harness 事实源](docs/design/MAGIC_POINTER_HARNESS_20260811.md)、[长任务历史审计](docs/2026-08-19-LONG_RUN_CAPABILITY_GAP.md)。长任务硬超时与 90 轮上限是历史问题，不能据旧审计再实现一次。
- 本机精确编辑记录：`data/runtime/harness-evals/precise-edit-repeat-check/report.json`；当前循环、工具、上下文与任务日志实现：`electron/runtime/agent.ts`、`tools.ts`、`agent_services.ts`、`session.ts`。本 PRD 没有运行新的模型或产品测试。
- 用户约束来自 [Grok 分享对话](https://x.com/i/grok/share/13c91c933dfb4b5fa50f3931d19cf134)。竞品自述参考 [Today](https://today.ai/)、[Vida](https://vida.app/desktop/)、[Grok Bot](https://docs.x.ai/grok-bot/overview)。厂商描述只说明方向，不证明真实成功率；本 PRD 的取舍以 MP 的用户任务和可验收结果为准。
